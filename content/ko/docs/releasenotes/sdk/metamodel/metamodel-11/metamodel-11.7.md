---
title: "11.7"
url: /releasenotes/sdk/metamodel-11.7/
weight: 64
---

## 11.7.0

### Workflows

#### NotifyTarget (Element)

* 이 엘리먼트를 도입하였습니다. 

#### NotifyWaitForNotificationActivityTarget (Element)

* 이 엘리먼트를 도입하였습니다. 

### Microflows

#### NotifyWorkflowAction (Element)

* `activity` 속성을 삭제하였습니다. 
* `notifyTarget` 속성을 도입하였습니다. 
* `targetId` 속성을 도입하였습니다. 

### ExcelDataImporter

#### Template (ModelUnit)

* 이 모델 유닛을 도입하였습니다.

#### TemplateContents (Element)

* 이 엘리먼트를 도입하였습니다.

#### ExcelTemplateContents (Element)

* 이 엘리먼트를 도입하였습니다.

#### CSVTemplateContents (Element)

* 이 엘리먼트를 도입하였습니다.

#### ExcelSheet (Element)

* 이 엘리먼트를 도입하였습니다.

#### CSVSheet (Element)

* 이 엘리먼트를 도입하였습니다.

#### ColumnAttributeMapping (Element)

* 이 엘리먼트를 도입하였습니다.

#### DataImporterElement (Element)

* 이 엘리먼트를 도입하였습니다.

#### Reference (Element)

* 이 엘리먼트를 도입하였습니다.

#### IndexReference (Element)

* 이 엘리먼트를 도입하였습니다.

#### NameReference (Element)

* 이 엘리먼트를 도입하였습니다.

#### ImportExcelDataAction (Element)

* 이 엘리먼트를 도입하였습니다.

#### ExcelSheetMappingSourceReference (Element)

* 이 엘리먼트를 도입하였습니다.

#### CsvSheetMappingSourceReference (Element)

* 이 엘리먼트를 도입하였습니다.
