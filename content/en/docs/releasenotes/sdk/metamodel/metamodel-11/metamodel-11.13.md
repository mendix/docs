---
title: "11.13"
url: /releasenotes/sdk/metamodel-11.13/
weight: 54
---

## 11.13.0

### Workflows

#### TimerEventSubProcessStartActivity (Element)

* We introduced this element. 

#### NonInterruptingTimerEventSubProcessStartActivity (Element)

* We introduced this element. 

#### InterruptingTimerEventSubProcessStartActivity (Element)

* We introduced this element. 

### Microflows

#### EmailMessage (Element)

* We deleted the `subject` property. Info: "Use subjectTemplate instead".
* We deleted the `messageBodyPlainText` property. Info: "Use messageBodyPlainTextTemplate instead".
* We deleted the `messageBodyHtml` property. Info: "Use messageBodyHtmlTemplate instead".

### Reports

#### ReportWidget (Element)

* We deleted this element. 

#### BasicReport (Element)

* We deleted this element. 

#### BasicReportAggregate (Element)

* We deleted this element. 

#### BasicReportColumn (Element)

* We deleted this element. 

#### ReportButton (Element)

* We deleted this element. 

#### ReportDateRangeField (Element)

* We deleted this element. 

#### ReportDateRangeSelector (Element)

* We deleted this element. 

#### ReportDropDown (Element)

* We deleted this element. 

#### ReportParameter (Element)

* We deleted this element. 

#### ReportZoomInfo (Element)

* We deleted this element. 

#### ReportZoomMapping (Element)

* We deleted this element. 

### DatabaseConnector

#### DatabaseQuery (Element)

* We deleted the `queryType` property. 
* We introduced the `type` property. 

### Texts

#### SystemTextCollection (ModelUnit)

* We introduced the `pluggableWidgetSystemTexts` property. 

### DataSets

#### DataSet (ModelUnit)

* We deleted the `dataSetAccess` property. 

#### DataSetAccess (Element)

* We deleted this element. 

#### DataSetConstraintAccess (Element)

* We deleted this element. 

#### DataSetModuleRoleAccess (Element)

* We deleted this element. 

#### DataSetObjectConstraint (Element)

* We deleted this element. 

#### DataSetParameterAccess (Element)

* We deleted this element. 
