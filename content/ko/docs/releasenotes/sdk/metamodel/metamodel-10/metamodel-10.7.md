---
title: "10.7"
url: /releasenotes/sdk/metamodel-10.7/
weight: 93
---

## 10.7.0

### Microflows

#### RestOperationCallAction (Element)

* 이 엘리먼트를 도입하였습니다.

#### RestOperationParameterMapping (Element)

* 이 엘리먼트를 도입하였습니다.

#### OutputVariable (Element)

* 이 엘리먼트를 도입하였습니다.

#### BodyVariable (Element)

* 이 엘리먼트를 도입하였습니다.

### Settings

#### WorkflowsProjectSettingsPart (Element)

* `onWorkflowEvent` 속성을 도입하였습니다. 

### ODataPublish

#### PublishedODataService2 (ModelUnit)

* `serviceNameRuntime` 속성을 도입하였습니다. 

### Rest

#### ConsumedRestService (ModelUnit)

* 이 모델 유닛을 도입하였습니다.

#### RestOperation (Element)

* 이 엘리먼트를 도입하였습니다.

#### ValueTemplate (Element)

* 이 엘리먼트를 도입하였습니다.

#### HeaderWithValueTemplate (Element)

* 이 엘리먼트를 도입하였습니다.

#### OperationParameter (Element)

* 이 엘리먼트를 도입하였습니다.

#### Value (Element)

* 이 엘리먼트를 도입하였습니다.

#### StringValue (Element)

* 이 엘리먼트를 도입하였습니다.

#### ConstantValue (Element)

* 이 엘리먼트를 도입하였습니다.

#### AuthenticationScheme (Element)

* 이 엘리먼트를 도입하였습니다.

#### BasicAuthenticationScheme (Element)

* 이 엘리먼트를 도입하였습니다.

#### RestOperationResponseHandling (Element)

* 이 엘리먼트를 도입하였습니다.

#### NoResponseHandling (Element)

* 이 엘리먼트를 도입하였습니다.

#### ImplicitMappingResponseHandling (Element)

* 이 엘리먼트를 도입하였습니다.

#### RestOperationMethod (Element)

* 이 엘리먼트를 도입하였습니다.

#### RestOperationMethodWithoutBody (Element)

* 이 엘리먼트를 도입하였습니다.

#### RestOperationMethodWithBody (Element)

* 이 엘리먼트를 도입하였습니다.

#### Body (Element)

* 이 엘리먼트를 도입하였습니다.

#### StringBody (Element)

* 이 엘리먼트를 도입하였습니다.

#### ImplicitMappingBody (Element)

* 이 엘리먼트를 도입하였습니다.

### Pages

#### DataView (Element)

* `editable` 속성을 삭제하였습니다. Info: "Use property 'editability' instead"
* `editability` 속성을 도입하였습니다. 
* `conditionalEditabilitySettings` 속성을 도입하였습니다. 

### Workflows

#### Workflow (ModelUnit)

* `onWorkflowEvent` 속성을 도입하였습니다. 

#### WorkflowEventHandler (Element)

* 이 엘리먼트를 도입하였습니다. 
