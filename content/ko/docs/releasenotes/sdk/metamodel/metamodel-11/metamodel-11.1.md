---
title: "11.1"
url: /releasenotes/sdk/metamodel-11.1/
weight: 76
---

## 11.1.0

### Workflows

#### Workflow (ModelUnit)

* `workflowMetaData` 속성을 도입하였습니다. 
* `workflowV2` 속성을 도입하였습니다. 

#### WorkflowActivity (Element)

* `relativeMiddlePoint` 속성을 도입하였습니다. 
* `^size` 속성을 도입하였습니다. 

#### StartWorkflowActivity (Element)

* 이 엘리먼트를 도입하였습니다. 

#### WorkflowMetaData (Element)

* 이 엘리먼트를 도입하였습니다. 

#### FloatingAnnotation (Element)

* 이 엘리먼트를 도입하였습니다. 

#### FlowLine (Element)

* 이 엘리먼트를 도입하였습니다. 

#### Line (Element)

* 이 엘리먼트를 도입하였습니다. 

#### OrthogonalPath (Element)

* 이 엘리먼트를 도입하였습니다. 

#### BezierCurve (Element)

* 이 엘리먼트를 도입하였습니다. 

### Pages

#### Page (ModelUnit)

* `autofocus` 속성을 도입하였습니다. 

#### PageParameterMapping (Element)

* `variable` 속성을 선택 사항으로 변경하였습니다.
* `variable` 속성의 기본값을 변경하였습니다.

### Navigation

#### NativeNavigationProfile (Element)

* `nativeNavigationEnabled` 속성을 도입하였습니다. 

### ODataPublish

#### CallMicroflowToChange (Element)

* `microflow` 속성을 선택 사항으로 변경하였습니다.
* `microflowRuntime` 속성을 도입하였습니다. 

### DatabaseConnector

#### ExecuteDatabaseQueryAction (Element)

* `connectionParameterMappings` 속성을 도입하였습니다. Info: "Connection parameters to set dynamic connection properties in the activity."

#### ConnectionParameterMapping (Element)

* 이 엘리먼트를 도입하였습니다. Info: "Connection parameters to set dynamic connection properties in the activity."
