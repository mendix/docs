---
title: "11.5"
url: /releasenotes/sdk/metamodel-11.5/
weight: 68
---

## 11.5.0

### Workflows

#### FlowLine (Element)

* `caseValues` 속성을 도입하였습니다. 

#### MergeActivity (Element)

* 이 엘리먼트를 도입하였습니다. 

#### CaseValue (Element)

* 이 엘리먼트를 도입하였습니다. 

#### StringCase (Element)

* 이 엘리먼트를 도입하였습니다. 

#### BooleanCase (Element)

* 이 엘리먼트를 도입하였습니다. 

#### VoidCase (Element)

* 이 엘리먼트를 도입하였습니다. 

### Pages

#### PageParameter (Element)

* `isRequired` 속성을 도입하였습니다. 
* `defaultValue` 속성을 도입하였습니다. 

#### CallNanoflowClientAction (Element)

* `outputMappings` 속성을 도입하였습니다. 

#### MicroflowSettings (Element)

* `outputMappings` 속성을 도입하였습니다. 

#### OutputMapping (Element)

* 이 엘리먼트를 도입하였습니다. 

### Microflows

#### ExternalActionParameterMapping (Element)

* `additionalAttributes` 속성을 도입하였습니다. 

#### AdditionalAttribute (Element)

* 이 엘리먼트를 도입하였습니다. Info: "For open types"

#### AuthenticationDocumentConfig (Element)

* 이 엘리먼트를 도입하였습니다. 

### Rest

#### ODataEntityTypeSource (Element)

* `isOpen` 속성을 도입하였습니다. Info: "Indicates that the service allows properties to be specified that are not in the metadata"

### Authentication

#### OAuth20AuthenticationDetails (Element)

* `clientId` 속성을 선택 사항으로 변경하였습니다.
* `clientSecret` 속성을 선택 사항으로 변경하였습니다.
* `tokenEndPoint` 속성을 선택 사항으로 변경하였습니다.
* `audience` 속성을 도입하였습니다.
