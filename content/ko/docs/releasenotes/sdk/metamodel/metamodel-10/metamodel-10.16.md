---
title: "10.16"
url: /releasenotes/sdk/metamodel-10.16/
weight: 84
---

## 10.16.0

### Microflows

#### Flow (Element)

* `originBezierVector` 속성을 삭제하였습니다. 
* `destinationBezierVector` 속성을 삭제하였습니다. 
* `lineType` 속성을 삭제하였습니다. 
* `line` 속성을 도입하였습니다. 

#### Line (Element)

* 이 엘리먼트를 도입하였습니다. 

#### BezierCurve (Element)

* 이 엘리먼트를 도입하였습니다. 

#### OrthogonalPath (Element)

* 이 엘리먼트를 도입하였습니다. 

### Mappings

#### MappingDocument (ModelUnit)

* `mappingSourceReference` 속성을 도입하였습니다. Info: "Stores the mapping source."

#### MappingSourceReference (Element)

* 이 엘리먼트를 도입하였습니다. Info: "A generic way to store the mapping source."

### Workflows

#### BoundaryEvent (Element)

* `annotation` 속성을 도입하였습니다. 
