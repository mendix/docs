---
title: "11.2"
url: /releasenotes/sdk/metamodel-11.2/
weight: 74
---

## 11.2.0

### Workflows

#### UserTaskActivity (Element)

* We deleted the `userSource` property. 
* We introduced the `userTargeting` property. 

#### UserSource (Element)

* We deleted this element. 

#### EmptyUserSource (Element)

* We deleted this element. 

#### XPathBasedUserSource (Element)

* We deleted this element. 

#### MicroflowBasedUserSource (Element)

* We deleted this element. 

#### UserTargeting (Element)

* We introduced this element. 

#### NoUserTargeting (Element)

* We introduced this element. 

#### XPathBasedTargeting (Element)

* We introduced this element. 

#### XPathUserTargeting (Element)

* We introduced this element. 

#### XPathGroupTargeting (Element)

* We introduced this element. 

#### MicroflowBasedTargeting (Element)

* We introduced this element. 

#### MicroflowUserTargeting (Element)

* We introduced this element. 

#### MicroflowGroupTargeting (Element)

* We introduced this element. 

### Settings

#### WorkflowsProjectSettingsPart (Element)

* We introduced the `groups` property. 

#### WorkflowGroup (Element)

* We introduced this element. 

### Microflows

#### SendEmailAction (Element)

* We introduced this element. 

#### AuthenticationConfig (Element)

* We introduced this element. 

#### BasicAuthConfig (Element)

* We introduced this element. 

#### EmailConnectionConfig (Element)

* We introduced this element. 

#### EmailMessage (Element)

* We introduced this element.
